# SSH Map (NodeVisualSSH) v1.2.13 — зависимости для запуска
# Установка: pip install -r requirements.txt

PySide6>=6.5        # GUI framework (Qt 6): QGraphicsScene/View, QThread
paramiko>=3.0       # SSH-клиент (подключения и интерактивный терминал)
keyring>=24.0       # Системное хранилище паролей (Credential Manager)
pyte>=0.8.0         # ANSI-эмуляция терминала v0.8 (modules/terminal_screen.py — жёсткая зависимость)
wcwidth>=0.2.9      # ширина Unicode-символов (modules/terminal_widget.py импортирует напрямую; транзитивно через pyte без пина — v1.2.10 AUDIT ручной #4)

# Опционально — backend keyring под Windows (без него пароль не переживёт перезапуск):
# pywin32
# Альтернатива: keyrings-cryptunprotect-backend
