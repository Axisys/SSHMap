# -*- coding: utf-8 -*-
"""Единая точка истины для версии приложения (AUDIT v0.8.3, средняя #1).

Ранее версия была размазана по main.py-логу, i18n-ключу title.main_window,
полю version в JSON проекта (storage/project.py) и комментариям — что уже
приводило к рассинхронизации (код говорил v0.8.1 при релизе v0.8.3).

Теперь все потребители импортируют отсюда:

    from version import APP_VERSION          # плоский запуск (из корня)
    from ..version import APP_VERSION        # из пакета

VERSION_FORMAT хранит версию ФОРМАТА JSON проекта (меняется только при
реальном изменении схемы; формат не обязан совпадать с релизом).
"""

APP_VERSION = "1.1.1"        # релиз приложения (лог старта, заголовок окна) — v1.1.1: мелкие опции вокруг хаба: шрифты UI/терминала с применением на лету, английский по умолчанию (новые пользователи), лимит своих терминалов = 4 (предложение закрыть старейшую), двойной клик узла (свойства/диалог входа), скрытие кнопочного блока сайдбара + пункт меню «Вид → Сайдбар», плашка связи: тип на плашке + лимит 20 символов метки; v1.1: диалог настроек (хаб): QTabWidget 6 вкладок, точки входа (меню «Настройки» + кнопка ⚙ сайдбара), ЕДИНЫЙ ~/.sshmap/config.json (миграция external_terminal из ~/.sshmap_settings.json), terminal_close_behavior (close/ask), настройки StatusChecker и автосохранения, язык с немедленным применением; v1.0: Терминал v1 завершён (RC1–RC4 + финал): посячейный холст QWidget+QPainter, полная клавиатура + выделение/копирование, resize PTY, скроллбэк pyte.HistoryScreen, Быстрый запуск; ключи terminal_* в ~/.sshmap/config.json + полный acceptance tests/test_terminal_acceptance.py
APP_NAME = "SSH Map"         # базовое имя (заголовок окна)
VERSION_FORMAT = "0.9"       # версия формата JSON проекта (+ "background", storage/project.py)
